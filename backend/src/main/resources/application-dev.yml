spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:127.0.0.1}:${DB_PORT:3306}/cpt202_consultancy?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Root@123}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: ${REDIS_HOST:127.0.0.1}
      port: ${REDIS_PORT:9002}
      password: ${REDIS_PASSWORD:}
      database: ${REDIS_DATABASE:0}
      timeout: ${REDIS_TIMEOUT:5000ms}

  devtools:
    restart:
      enabled: true

server:
  port: 8081
# JWT config
jwt:
  secret: dev-secret-key-with-at-least-32-characters

# API
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

# log
logging:
  level:
    root: INFO
    edu.xjtlu.cpt202: DEBUG
