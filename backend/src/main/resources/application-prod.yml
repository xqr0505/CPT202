spring:
  datasource:
    url: jdbc:mysql://db:3306/${MYSQL_DATABASE}?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: ${MYSQL_ROOT_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  devtools:
    restart:
      enabled: false

jwt:
  secret: ${JWT_SECRET}

server:
  port: 8080

logging:
  level:
    root: INFO
    edu.xjtlu.cpt202: INFO